import 'package:flutter/material.dart';

/// Lists the user's TODOs items.
class NotesPage extends StatelessWidget {
  /// Ctor.
  const NotesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Notes');
  }
}
