import 'package:flutter/material.dart';

/// A listing of the most recent news.
class NewsPage extends StatelessWidget {
  /// Ctor.
  const NewsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('News');
  }
}
